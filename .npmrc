# The bug-reporter SDK declares a peer range for an older lucide-react (0.4x-0.5x)
# while this app uses lucide-react 1.x across all UI/nav icons (stable icon API).
# Accept the mismatch so `npm install` succeeds on Vercel/CI and fresh clones.
legacy-peer-deps=true
